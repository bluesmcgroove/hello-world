print "Simple addition"
varx = raw_input("First number? ")
vary = raw_input("Second number? ")

varsum = float(varx)+float(vary)

print "Adding %r and %r gives result of %r." % (
	varx, vary, varsum)